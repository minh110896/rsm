<% if flash[:success].present? %>
  $('#job-<%= params[:id] %>').hide().after('<%= j render(@job) %>');
  $('#myModal').modal('hide');
  <% flash.clear %>
<% else %>
  $('#myModal').modal('hide');
  <% flash.clear %>
<% end %>
