<% if @certificate.errors.any? %>
  alertify.error('<%= j (render partial: "shared/error_messages", locals: {object: @certificate}) %>');
<% else %>
  $('#new-certificate').remove();
  $('#btn-add-certificate').show();
  $('#form-certificate').append('<%= j render(@certificate) %>');
<% end %>

